# 工作日志 · 2026-05-08

> 按条目合并：同一 bug 多次修复追加到同一条目；同一功能多轮开发追加到同一条目；工时累计估算。

## 🐛 Bug 修复

（暂无）

## ✨ 功能开发

### [F1] 视频库播放面板缩略图与队列改进

- **预估工时**：4h
- **关联文档**：无
- **涉及文件**：
  - `frontend/src/features/video-library/components/VideoPlayerPanel.tsx`
  - `frontend/src/features/video-library/components/VideoListPanel.tsx`
  - `frontend/src/features/video-library/pages/VideoLibraryPage.tsx`
  - `frontend/src/features/video-playback/VideoPlayer.tsx`
  - `frontend/src/features/treesize/api.ts`
  - `tools/tool-treesize/.../api/TreeSizeController.java`
  - `tools/tool-treesize/.../service/FileDeleteService.java`
- **修改明细**：
  - `当前` 给移动端横向 strip 的每个缩略图加 Trash2 删除按钮，复用已有 onDelete prop
  - `当前` 移动端队列 grid 改为从 currentIndex 切片渲染，当前播放视频置首
  - `当前` VideoPlayer 加 controlsList="nofullscreen" 禁用原生全屏按钮
  - `当前` Panel 包一层 playerWrapperRef，监听 fullscreenchange 改造为容器全屏
  - `当前` 全屏外右上小全屏按钮，全屏时改为 flex-col + 底部独立控件条
  - `当前` 底部控件条 PotPlayer 风格：上一首 / 下一首 / 列表 / 退出全屏
  - `当前` 全屏播放列表抽屉 bottom-[68px] 让出底部控件条空间
  - `当前` VideoPlayer video 加 object-contain 防全屏比例失真
  - `当前` 隐藏当前播放项的删除按钮（避免误删 + Windows 文件锁）
  - `当前` 删除确认弹框新增完整路径行便于上主机核对
  - `当前` FileDeleteService fallback 永久删除走 WARN 日志，附 fallback 原因
  - `当前` TreeSizeController.deleteFile 返回 {toTrash} 替代 204
  - `当前` 前端按 toTrash=false 弹"已永久删除"提示并指向后端 WARN 日志
  - `当前` 删除前先切到下一首再 await，避免 Windows 锁阻 moveToTrash
