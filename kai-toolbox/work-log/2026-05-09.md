# 工作日志 · 2026-05-09

> 按条目合并：同一 bug 多次修复追加到同一条目；同一功能多轮开发追加到同一条目；工时累计估算。

## 🐛 Bug 修复

### [B1] Web 终端输入字符重复显示与方向键越过提示符

- **预估工时**：3.5h
- **关联文档**：[bug/Web终端/输入字符重复与方向键越过提示符/输入字符重复与方向键越过提示符.md](../bug/Web终端/输入字符重复与方向键越过提示符/输入字符重复与方向键越过提示符.md)
- **涉及文件**：
  - `frontend/src/features/webterm/components/Terminal.tsx`
  - `frontend/src/features/webterm/pages/WebTermPage.tsx`
  - `tools/tool-webterm/.../service/ShellLauncher.java`
  - `tools/tool-webterm/.../session/WebTermSession.java`
  - `tools/tool-webterm/.../handler/WebTermSocketHandler.java`
  - `pom.xml` / `tools/tool-webterm/pom.xml`
  - `design/Web终端/Web终端-current.md` / `Web终端-coding.md`
- **修改明细**：
  - `当前` 第一轮 Terminal.tsx 删 `term.write(data)` 本地 echo + fit 改 rAF/尺寸校验
  - `当前` 第一轮 ShellLauncher 取消嵌套子 PowerShell + `-NoExit -Command` UTF-8 编码
  - `当前` 第一轮后实测仍退格/方向键/Tab 全失效，根因 PowerShell 无 PTY 时 PSReadLine 不启用
  - `当前` 第二轮 父 pom 加 JetBrains Maven repo + dependencyManagement pty4j 0.12.25
  - `当前` 第二轮 ShellLauncher 改 PtyProcessBuilder + setUseWinConPty(true)，env 设 TERM=xterm-256color
  - `当前` 第二轮 WebTermSession 持 PtyProcess；setSize 调 setWinSize 真下发；只起 1 条转发线程
  - `当前` 第二轮 WebTermSocketHandler.handleOpen 改 launch(shell,cwd,cols,rows) 传初始尺寸
  - `当前` 第二轮 WebTermPage 提示文案更新（不再声明"不接入 PTY"）
  - `当前` 第二轮 设计文档 §1.2/§1.3/§3.1/§7.2/§8 RK1-RK3/§9 全面修订；coding.md §3.5/§3.7 同步

## ✨ 功能开发

### [F1] 视频库支持删除当前播放视频（主动断流）

- **预估工时**：1.5h
- **关联文档**：无
- **涉及文件**：
  - `frontend/src/features/video-library/components/VideoPlayerPanel.tsx`
  - `frontend/src/features/video-library/components/VideoListPanel.tsx`
  - `frontend/src/features/video-library/pages/VideoLibraryPage.tsx`
  - `tools/tool-treesize/.../service/FileDeleteService.java`
- **修改明细**：
  - `当前` 撤销 5-08 隐藏当前播放项删除按钮，三处恢复显示（Panel strip / Panel grid / VideoListPanel）
  - `当前` handleDelete 切下一首后等 400ms 让 hls.js destroy + GET 关闭 + ffmpeg reap
  - `当前` FileDeleteService 加重试：moveToTrash 失败短退避 200/400ms 重试 3 轮
  - `当前` Files.delete fallback 同样 3 轮重试，覆盖 Windows 锁释放延迟
  - `当前` 提取 TrashOutcome record + sleepQuiet 私有方法清理控制流

### [F2] 视频列表多选 + 批量删除

- **预估工时**：2.5h
- **关联文档**：无
- **涉及文件**：
  - `frontend/src/features/video-library/components/VideoListPanel.tsx`
  - `frontend/src/features/video-library/components/VideoPlayerPanel.tsx`
  - `frontend/src/features/video-library/pages/VideoLibraryPage.tsx`
  - `toolbox-starter/src/main/resources/application.yml`
- **修改明细**：
  - `当前` VideoListPanel 加 multiSelectMode + selectedPaths state，items 变更时自动剪除幻影
  - `当前` 头部工具栏：普通态显示"多选"按钮；多选态显示选中计数 / 全选 / 删除 / 退出
  - `当前` 多选模式下点行切换勾选不再切歌，行左侧显示 CheckSquare/Square，行高亮
  - `当前` 多选态隐藏单行删除按钮，避免误触
  - `当前` 新增 onBulkDelete prop，VideoLibraryPage 实现 handleBulkDelete
  - `当前` 批量 confirm 弹窗显示总大小 + 路径列表 (cap 30) + 含播放视频提示
  - `当前` 含播放视频时先切下一首 + 等 400ms 再批量删除（复用断流策略）
  - `当前` 顺序调用 deleteFile，聚合 success/perma/error，弹结果 summary
  - `当前` VideoPlayerPanel 队列 grid 也加多选（独立 state），头部多选/全选/删除/退出工具栏
  - `当前` 队列卡片左上角 checkbox 角标，多选态隐藏单删除按钮
  - `当前` 应用 yml 移除默认 mts 扩展（与 TypeScript ESM .d.mts 撞名，磁盘上几乎全是噪音）

### [F3] 视频画面内删除当前播放入口

- **预估工时**：0.25h
- **关联文档**：无
- **涉及文件**：
  - `frontend/src/features/video-library/components/VideoPlayerPanel.tsx`
- **修改明细**：
  - `当前` 非全屏视频右上 Maximize 旁加 Trash2 按钮，hover 红色，调 onDelete(item)
  - `当前` 全屏底部控件条「列表」与「退出」之间加「删除」按钮，红色调，复用 onDelete

### [F5] 邮件收件服务器服务端代码优化（bug 修复 + 健壮性 + 批量/状态/白名单）

- **预估工时**：2h
- **关联文档**：[design/邮件收件服务器/邮件收件服务器-current.md](../design/邮件收件服务器/邮件收件服务器-current.md) · [DNS 部署手册](../design/邮件收件服务器/邮件收件服务器-DNS部署手册.md)
- **涉及文件**：
  - `tools/tool-mail/.../config/MailProperties.java`
  - `tools/tool-mail/.../config/MailConfig.java`（新增）
  - `tools/tool-mail/.../config/SmtpServerManager.java`
  - `tools/tool-mail/.../smtp/MailMessageHandler.java`
  - `tools/tool-mail/.../smtp/MimeMessageParser.java`（新增）
  - `tools/tool-mail/.../repository/MailInboxRepository.java`
  - `tools/tool-mail/.../api/MailController.java`
  - `tools/tool-mail/.../api/dto/MailServerStatusView.java`（新增）
  - `tools/tool-mail/.../api/dto/MailIdBatchRequest.java`（新增）
  - `tools/tool-mail/.../api/dto/MailBatchResultView.java`（新增）
  - `toolbox-starter/src/main/resources/application.yml`
- **修改明细**：
  - `当前` 修 from() 信封地址未去尖括号 + 白名单后缀匹配 bug（`@amazon.com` 走精确匹配永远失配）
  - `当前` 修 data() 解析异常被吞导致对方 250 OK 但邮件丢失，改抛 RejectException 451 触发重投
  - `当前` 修 inline 非文本（截图等）整段丢弃，归入附件元数据
  - `当前` 修 extractContent 用 append(getContent()) 隐式 toString 入库乱码风险
  - `当前` 抽 MimeMessageParser 工具类，handler 只管 SMTP 协议交互
  - `当前` MailConfig 单独 @Configuration 持有 EnableConfigurationProperties，匹配 MediaParserConfig 项目惯例
  - `当前` SmtpServerManager 设 maxConnections / maxRecipients / maxMessageSize 上限（公网 25 端口防刷）
  - `当前` SmtpServerManager 暴露 status() 含 enabled / running / port / hostname / startupError
  - `当前` MailProperties 新增 recipientDomainWhitelist + 三个 SMTP 上限字段
  - `当前` MailMessageHandler.recipient() 增加收件域白名单校验，未配置时全收
  - `当前` Repository markRead/deleteById 返回 boolean，省 Controller 一次 SELECT 存在性检查
  - `当前` Repository 新增 markReadBatch / deleteByIdsBatch（IN 子句拼接），返回实际命中行数
  - `当前` Controller markRead/delete 改 204 No Content + 404，去掉 Map.of("success",true) 不规范返回
  - `当前` Controller 新增 PATCH /inbox/batch/read、POST /inbox/batch/delete、GET /server/status
  - `当前` application.yml 新增配置项注释模板（receipt-domain-whitelist + SMTP 上限）

### [F4] Web 终端（浏览器交互式 Shell）

- **预估工时**：5h
- **关联文档**：[Web终端-current.md](../design/Web终端/Web终端-current.md) · [API](../design/Web终端/Web终端-api-current.md) · [coding](../design/Web终端/Web终端-coding.md)
- **涉及文件**：
  - `tools/tool-webterm/`（新建模块，9 个 Java 文件 + pom.xml）
  - `frontend/src/features/webterm/`（新建模块，5 个 ts/tsx 文件）
  - `pom.xml` / `toolbox-starter/pom.xml` / `frontend/package.json`
- **修改明细**：
  - `当前` 写完整-技术设计文档 + API 协议 + coding.md，登记 design INDEX
  - `当前` 新建 tool-webterm Maven 模块，引入 spring-boot-starter-websocket
  - `当前` ShellLauncher 仅 Windows，PowerShell 强制 UTF-8、cmd `chcp 65001`
  - `当前` WebTermSession：Process + WSSession 桥接，虚拟线程读 stdout/stderr，8KB/50ms 触发推送
  - `当前` WebTermSocketHandler：路由 open/input/resize/close 四类消息，open 5s 超时关连接
  - `当前` WebTermSessionRegistry：ConcurrentHashMap + maxSessions=8 + @PreDestroy 清理
  - `当前` ClientMessage/ServerMessage 用 sealed interface + Jackson `@JsonSubTypes`
  - `当前` 父 pom + starter pom 注册新模块依赖
  - `当前` 前端 useWebTermSocket hook 封装 WS 生命周期 + JSON 编解码
  - `当前` Terminal.tsx 用 @xterm/xterm + @xterm/addon-fit + ResizeObserver
  - `当前` 本地 echo 兜底 RK2（无 PTY 时 Shell 不主动回显输入）
  - `当前` WebTermPage 提供 PowerShell/cmd 切换 + cwd 输入 + 重新连接按钮
  - `当前` mvn -pl toolbox-starter -am compile + npm run typecheck 全通过
