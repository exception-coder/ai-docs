# 工作日志 · 2026-05-11

> 按条目合并：同一 bug 多次修复追加到同一条目；同一功能多轮开发追加到同一条目；工时累计估算。

## ✨ 功能开发

### [F1] 项目管理面板 + Web 终端 deep-link autorun claude

- **预估工时**：2.5h
- **关联文档**：[design/项目管理/项目管理-current.md](../design/项目管理/项目管理-current.md) · [API](../design/项目管理/项目管理-api-current.md) · [coding](../design/项目管理/项目管理-coding.md)
- **涉及文件**：
  - `tools/tool-projects/`（新建模块，10 个 Java 文件 + pom.xml）
  - `frontend/src/features/projects/`（新建模块，7 个 ts/tsx 文件）
  - `frontend/src/features/webterm/pages/WebTermPage.tsx`
  - `frontend/src/features/webterm/components/Terminal.tsx`
  - `pom.xml` / `toolbox-starter/pom.xml` / `application.yml`
- **修改明细**：
  - `当前` 写完整-业务设计文档 + API + coding.md，登记到 design INDEX
  - `当前` 父 pom 注册 tool-projects 模块 + dependencyManagement 坐标
  - `当前` toolbox-starter pom 增加 tool-projects 依赖
  - `当前` application.yml 新增 toolbox.projects.{root,cache-ttl-seconds,hidden-prefixes}
  - `当前` 新建 tool-projects Maven 模块，依赖 toolbox-common + spring-boot-starter-web + validation
  - `当前` ProjectsProperties 与 WebTermProperties 同款 class + Lombok 风格
  - `当前` ProjectsDescriptor 实现 ToolDescriptor，id=projects icon=folder-git-2 order=10
  - `当前` ProjectType 枚举 7 类（flutter/maven/gradle/node/python/git/other），小写序列化对齐前端字面量
  - `当前` ProjectInfo / ProjectsListResponse / OpenInExplorerRequest 三个 record DTO，全 Javadoc
  - `当前` ProjectTypeDetector 静态优先级首命中：flutter > maven > gradle > node > python > git > other
  - `当前` ProjectScanner：一级目录扫 + 隐藏前缀过滤 + .git/HEAD 静默读分支 + mtime 倒序
  - `当前` ProjectsCache：volatile + 双检锁的单条目 TTL 缓存，不引 Caffeine 新依赖
  - `当前` ProjectsController：GET /api/projects 命中缓存；POST /api/projects/open 强校验 path.startsWith(root) 防越权 + Desktop.open 兜 HeadlessException 503
  - `当前` 路径越权与桌面环境异常分别用 IllegalArgumentException / ResponseStatusException 转译，复用 GlobalExceptionHandler 统一格式
  - `当前` Path.of 捕获精确化为 InvalidPathException
  - `当前` 前端 features/projects manifest icon=FolderGit2 order=5（避开 treesize order=10，作为日常入口排在最前）
  - `当前` ProjectsPage：搜索框 + 网格 auto-fill minmax(280px,1fr) + 加载/空态/根目录不存在提示
  - `当前` ProjectCard：整块点击跳转 /tools/webterm?cwd=...&autorun=claude；hover 露三按钮（终端、复制路径、文件管理器）；stopPropagation 隔离次要按钮
  - `当前` ProjectTypeBadge：7 种类型 → lucide 图标 + Tailwind 颜色映射
  - `当前` useProjects 用 TanStack Query staleTime=5_000 + refetchOnWindowFocus
  - `当前` API 走 @/lib/api 的 http 封装，与 treesize 风格一致（项目里没有 axios）
  - `当前` WebTermPage 增 useSearchParams 解析 cwd/autorun，autorun 严格白名单 ['claude']
  - `当前` WebTermPage：手动改 cwd 输入框或点击重连时 setAutorun(null)，避免「改路径仍 autorun」的意外
  - `当前` Terminal 加 autorun?: string|null prop；useEffect 监听 socket.state==='ready' 延后 80ms 追发 `${autorun}\r\n`，避开与 PowerShell PS1 渲染竞速
  - `当前` 不修改 useWebTermSocket / Web 终端后端契约，跨模块协作仅靠前端 query + ready 钩子
  - `当前` design current.md §4.1 / coding.md §3.1.2 §3.2 §3.3 同步反映现状（不改 hook、icon order=5、http 封装）
  - `当前` npm run typecheck 通过；mvn 本机未装，待 IDE 编译验证
