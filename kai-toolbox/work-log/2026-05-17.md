# 工作日志 · 2026-05-17

> 按条目合并：同一 bug 多次修复追加到同一条目；同一功能多轮开发追加到同一条目；工时累计估算。

## 🐛 Bug 修复

（暂无）

## ✨ 功能开发

### [F1] [重构] formatter JSON 子面板大文件优化（CodeMirror + Web Worker）

- **预估工时**：9h
- **关联文档**：[../design/JSON格式化大文件优化/JSON格式化大文件优化-current.md](../design/JSON格式化大文件优化/JSON格式化大文件优化-current.md) · [coding](../design/JSON格式化大文件优化/JSON格式化大文件优化-coding.md)
- **涉及文件**：
  - `frontend/src/features/formatter/lib/json-worker.ts`
  - `frontend/src/features/formatter/lib/useJsonWorker.ts`
  - `frontend/src/features/formatter/components/JsonEditor.tsx`
  - `frontend/src/features/formatter/components/JsonPanel.tsx`
  - `frontend/src/lib/useIsDarkTheme.ts`
  - `frontend/src/features/browser-request/pages/BrowserRequestPage.tsx`
  - `frontend/src/features/formatter/lib/jsonToFlow.ts`（新增）
  - `frontend/src/features/formatter/components/JsonTreeView.tsx`（新增）
- **修改明细**：
  - `22:30` 新增 lib/json-worker.ts：format / minify / escape / unescape 四 op + SyntaxError position 提取
  - `22:35` 新增 lib/useJsonWorker.ts：懒加载 Worker、id 关联 pending、unmount 兜底、worker 异常 fallback 到同步实现
  - `22:42` 新增 components/JsonEditor.tsx：CodeMirror 包装，uncontrolled + ref API（getValue/setValue/focusError），高亮按字节阈值切换
  - `22:48` 重构 components/JsonPanel.tsx：去 textarea/input state，输入输出双编辑器，busy 锁，>8MB 隐藏复制改下载
  - `22:50` 抽 lib/useIsDarkTheme.ts 到共享目录，BrowserRequestPage 改用 import
  - `22:55` typecheck 通过；Vite dev server 启动正常，6 模块均 200 返回；worker 由 Vite 编译为 ?worker_file&type=module
  - `23:08` 布局改双栏：FormatterPage max-w 放宽到 1600px；JsonPanel 顶部缩进+按钮一行、下方输入/输出 lg:grid-cols-2；编辑器吃满 calc(100vh - 320px)
  - `23:08` JsonEditor 新增 maxHeight prop，传给 CodeMirror 让大文件出现内部滚动条而不撑外页
  - `23:18` 装 @xyflow/react；新增 lib/jsonToFlow.ts：JSON 转 React Flow nodes/edges + 简易层级布局；硬上限 1000 节点
  - `23:18` 新增 components/JsonTreeView.tsx：自定义 JsonNode 渲染器（header + rows + 每行 source handle），背景/控制条/Minimap，主题跟随
  - `23:21` JsonPanel 加 viewMode segment（文本/图形）；树视图懒渲染，输出 > 4MB 不自动转图给提示，parse 失败/overflow 各自占位
  - `23:21` 修 TS 报错：JsonNodeData extends Record<string, unknown> 满足 React Flow v12 Node["data"] 约束
  - `23:33` jsonToFlow 重构为 parseJson + buildFlow(root, {expanded})；single 节点行上限 200，整图节点上限 500
  - `23:33` 新增 collectAllPaths 给「全展开」用；row 加 ChevronRight/ChevronDown 触发 onToggle，TreeContext 透到自定义节点
  - `23:34` JsonPanel 加 parsedRoot + expanded 状态、全展开/全收起按钮、展开节点数实时显示；每次新格式化重置回 root
  - `23:42` jsonToFlow 加 NODE_VISUAL_MAX_HEIGHT=480 封顶 + LAYOUT_CONSTS 共享给 JsonNode；MAX_ROWS_PER_NODE 提到 2000
  - `23:42` JsonNode 加内部滚动；onScroll 经 rAF 节流后调 useUpdateNodeInternals(nodeId)，让节点内滚时 edge 端点跟着行同步对齐
  - `23:55` jsonToFlow 抽 PATH_SEP=\x1f + pathOf(parent,key)；jsonToFlow + worker 共用，避免 key 含点带来的 path id 歧义
  - `23:58` json-worker 实现 stringifyWithIndex(root, indent)：自写序列化器，与 JSON.stringify 输出字节一致，同步回填 IndexEntry[]（path + key/value offset 区间）
  - `00:00` worker format op 加 withIndex 选项；UI 默认带，索引随 res 返回；O(N) 时间 O(N) 内存
  - `00:01` JsonEditor 加 focusAt(from,to)：dispatch selection + EditorView.scrollIntoView(center)
  - `00:02` JsonTreeView 加 onJump：行 key 由 span 改为 button，点击触发跨视图跳转
  - `00:03` JsonPanel 加 pathIndex Map + handleJump：lookup → setViewMode('text') → microtask focusAt
  - `00:04` 设计文档加 9.7 跨视图跳转（含原理 flowchart + IndexEntry 结构 + 跳转流程）+ 第 10 节 v5–v10 路线图（含 v1→v10 串联图、各档难度评估、推荐顺序）
