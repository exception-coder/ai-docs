# 工作日志 · 2026-05-18

> 按条目合并：同一 bug 多次修复追加到同一条目；同一功能多轮开发追加到同一条目；工时累计估算。

## 🐛 Bug 修复

（暂无）

## ✨ 功能开发

### [F1] [续作] formatter v8 树视图搜索（接 2026-05-17 同主题）

- **预估工时**：2.5h
- **关联文档**：[../design/JSON格式化大文件优化/JSON格式化大文件优化-current.md](../design/JSON格式化大文件优化/JSON格式化大文件优化-current.md)（第 11 节）
- **涉及文件**：
  - `frontend/src/features/formatter/lib/searchInJson.ts`（新增）
  - `frontend/src/features/formatter/components/JsonTreeView.tsx`
  - `frontend/src/features/formatter/components/JsonPanel.tsx`
  - `frontend/src/features/formatter/lib/json-worker.ts`
  - `frontend/src/features/formatter/lib/useJsonWorker.ts`
- **修改明细**：
  - `00:15` 新增 lib/searchInJson.ts：DFS root，key/value 子串匹配，结果上限 5000 防御；附 ancestorPathsOf + parentPathOf 辅助
  - `00:22` JsonTreeView TreeContext 加 matchedPaths/currentMatchPath；JsonNode row 根据命中状态加 bg-amber 软/强双层高亮
  - `00:25` JsonTreeView 加 CenterOnEffect 子组件：useReactFlow().setCenter 等 rAF 一帧让 expand 后的新节点先布局；用 version 计数让同 nodeId 重复跳也触发
  - `00:28` JsonPanel 接入：250ms debounce 搜索；prev/next 沿 PATH_SEP 自动展开祖先 + setCenterOn(version++)；Enter / Shift+Enter / Esc 键盘
  - `00:30` 设计文档新增第 11 节 v8 实现记录（含 flowchart + 关键设计表 + setCenter 实现注释 + 已知限制）
  - `01:05` json-worker 加 parse op；WorkerRes ok 变体合并为带可选字段的单一形态，避免调用点 cast
  - `01:10` useJsonWorker.runSync 加 parse fallback；JsonPanel.refreshFromOutput 改 async + ticket 计数器保护并发
  - `01:13` TreeStatus 加 parsing 分支 + Loader2 spinner；TREE_SAFE_MAX_BYTES 4MB → 50MB 对齐文本视图
  - `01:16` 设计文档新增第 12 节：异步 parse 流程图 + 性能预算表 + 60% 主线程改善说明

### [F2] 新增个人秘书 feature 模块（移动端为主）

- **预估工时**：5h
- **关联文档**：[../design/个人秘书/个人秘书-current.md](../design/个人秘书/个人秘书-current.md) · [coding](../design/个人秘书/个人秘书-coding.md)
- **涉及文件**：
  - `frontend/src/features/secretary/index.tsx`
  - `frontend/src/features/secretary/types.ts`
  - `frontend/src/features/secretary/pages/SecretaryPage.tsx`
  - `frontend/src/features/secretary/components/{ComposerTabs,TextComposer,VoiceRecorder,AttachmentPicker,Timeline,EntryItem,EntryDetail}.tsx`
  - `frontend/src/features/secretary/lib/{entryRepo,createEntry,geo,format}.ts`
- **修改明细**：
  - `21:30` 写设计文档 个人秘书-current.md（完整-技术，含整体架构图 + 三种录入交互时序图 + 业务规则表）
  - `21:35` 写 coding.md：14 个类清单 + 方法签名 + IDB 结构 + 重要约束
  - `21:36` 登记 design/INDEX.md「个人秘书」条目
  - `21:40` 实现 types.ts：InputMethod + EntryGeo + Text/Voice/FileEntry 三个子类型联合
  - `21:42` 实现 lib/format.ts：formatDurationShort / groupKeyOf 今天昨天 / pickVoiceMime 协商
  - `21:44` 实现 lib/geo.ts：tryGetGeo 4s 超时，失败拒绝一律 null 不抛
  - `21:48` 实现 lib/entryRepo.ts：IDB 单例 + entries/blobs 双 store + byCreatedAt 索引 + 同事务删除
  - `21:50` 实现 lib/createEntry.ts：注入 id/createdAt/inputMethod/geo 的工厂
  - `21:55` 实现 TextComposer：matchMedia('(pointer: coarse)') 触屏不劫持 Enter；text-base 防 iOS 缩放
  - `22:00` 实现 VoiceRecorder：MediaRecorder + 计时器 + 卸载释放 stream + 权限拒绝静态提示
  - `22:05` 实现 AttachmentPicker：multiple 多选 + 预选列表移除 + 逐文件入库
  - `22:08` 实现 Timeline + EntryItem：今天/昨天/YYYY-MM-DD 分组 + 单列卡片 + 图标按方式 + 地点小字
  - `22:12` 实现 EntryDetail：Sheet 抽屉 + 小屏 !max-w-full + audio controls + 图片预览 + 下载按钮 + 撤销 ObjectURL
  - `22:18` 实现 SecretaryPage：sticky 顶部 composer + 折叠 + max-w-2xl 居中 + banner toast
  - `22:20` 注册 FeatureManifest：/tools/secretary，icon NotebookPen，group 内容工具，order 35
  - `22:25` 用户补充「移动端为主」反馈，TextComposer 增 coarse pointer 检测、EntryDetail 小屏全宽、按钮 size lg
  - `22:28` 设计文档补 4.5 节「移动端交互约束」+ coding 重要约束追加 4 行
  - `22:30` npm run typecheck 通过
  - `22:50` 用户反馈"某些设备点开始录音没反应"，重写 VoiceRecorder：增加 checkEnv 首屏预检（isSecureContext / mediaDevices / MediaRecorder）、按 DOMException.name 细分 8 种错误 code、每种 code 配标题+原因+多行建议、加重试按钮，杜绝静默失败
