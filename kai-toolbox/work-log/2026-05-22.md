# 2026-05-22 工作日志

## [功能开发] 视频改造 · 第 2 批（语言识别 + 九宫格预览图）

> 设计文档：
> - [视频语言识别](../design/视频库/视频语言识别/视频语言识别-current.md) · [api](../design/视频库/视频语言识别/视频语言识别-api-current.md) · [coding](../design/视频库/视频语言识别/视频语言识别-coding.md)
> - [视频九宫格预览图](../design/视频库/视频九宫格预览图/视频九宫格预览图-current.md) · [api](../design/视频库/视频九宫格预览图/视频九宫格预览图-api-current.md) · [coding](../design/视频库/视频九宫格预览图/视频九宫格预览图-coding.md)
>
> 累计工时：~3h

**复用既有基础设施（第 1 批已落地）：**
- `VideoProcessingJobService` 7 类任务统一调度 / `JobContext` / `recordSuccess|Failure` 不动
- `VideoTableRepository` 的 `countNeedingLanguageDetect / findNeedingLanguageDetect / updateLanguage / countNeedingThumbnailGrid / findNeedingThumbnailGrid / updateThumbnailGrid` 已经在第 1 批一次性预埋
- `ProcessingJobButton` 通用 React 组件 + `buildTaskApi(slug)` 模板，加按钮零样板

**通用层（toolbox-common）：**
- 改 [FfmpegProcessRegistry.java](../../../IdeaProjects/kai-toolbox/toolbox-common/src/main/java/com/exceptioncoder/toolbox/common/media/FfmpegProcessRegistry.java) 加两个高层方法：`extractAudioSlice(src, startSec, durSec, outWav)` 抽 16kHz 单声道 WAV（语言识别用）；`makeContactSheet(src, durationS, cols, rows, cellW, cellH, outJpg, timeoutS)` 单条 ffmpeg `fps=N/duration + scale + pad + tile=3x3` 一次出图。子进程统一走 `spawn()` 进程托管 + virtual thread drain stdout + `descendants().destroyForcibly()` 超时清理

**后端 tool-treesize：**
- 新增 [DetectedLanguage.java](../../../IdeaProjects/kai-toolbox/tools/tool-treesize/src/main/java/com/exceptioncoder/toolbox/treesize/domain/DetectedLanguage.java) record(iso, confidence)
- 改 [WhisperRunner.java](../../../IdeaProjects/kai-toolbox/tools/tool-treesize/src/main/java/com/exceptioncoder/toolbox/treesize/service/WhisperRunner.java) 加 `detectLanguage(wav, cancelled)` 方法 + `LANGUAGE_RE_WITH_P` 捕 ISO+p；命令构造独立于 `buildCommand`（只保留 model/file/GPU 三件套，所有 transcription 专用 flag 不带，避免 build 间冲突）；`--detect-language` 字符串硬编码（whisper.cpp 各 build 稳定名）
- 新增 [VideoLanguageDetectionService.java](../../../IdeaProjects/kai-toolbox/tools/tool-treesize/src/main/java/com/exceptioncoder/toolbox/treesize/service/VideoLanguageDetectionService.java) — batch 50 + 25% 偏移 60s 抽样 + `< 5s` 标 too_short + `recentlyActive(15s)` 礼让 + `{tmpdir}/kai-toolbox/lang-detect/` 临时 wav 用完即删 + 启动时 `@PostConstruct` 清残留；asr-service 模式直接拒（503）；start() 检 binary/model 存在则抛 IllegalStateException
- 新增 [VideoThumbnailGridService.java](../../../IdeaProjects/kai-toolbox/tools/tool-treesize/src/main/java/com/exceptioncoder/toolbox/treesize/service/VideoThumbnailGridService.java) — 缓存目录复用 `ThumbnailService` 解析策略：`{thumb.cache-dir}/grid/` + fallback `~/.kai-toolbox/cache/thumbs/grid/`；输出文件名 `{sha256(absPath)[0:16]}.jpg`；缓存命中（文件存在但库字段 NULL）直接 UPDATE 不重跑 ffmpeg；`< 2s` 标 too_short
- 改 [VideoTableRepository.java](../../../IdeaProjects/kai-toolbox/tools/tool-treesize/src/main/java/com/exceptioncoder/toolbox/treesize/repository/VideoTableRepository.java) 加 `findGridPathByVideoPath(path)` —— 取图端点查 grid_path 用，过滤掉 NULL 字段
- 改 [VideoProcessingController.java](../../../IdeaProjects/kai-toolbox/tools/tool-treesize/src/main/java/com/exceptioncoder/toolbox/treesize/api/VideoProcessingController.java) — 注入两个新 service；加 `/language-detect/{start,stop,status,events}` 4 端点 + `/thumbnail-grid/{start,stop,status,events}` 4 端点 + `GET /thumbnail-grid?path=` 取图端点（`StreamingResponseBody` + `Cache-Control: 1d` + 404 兜底）；顶部注释从「3 类落地」更到「5 类落地」

**前端 video-library：**
- 改 [api.ts](../../../IdeaProjects/kai-toolbox/frontend/src/features/video-library/api.ts) 加 `languageDetectApi = buildTaskApi('language-detect')` / `thumbnailGridApi = buildTaskApi('thumbnail-grid')` + `thumbnailGridUrl(path)` 给 `<img src>` 用
- 改 [VideoProcessingToolbar.tsx](../../../IdeaProjects/kai-toolbox/frontend/src/features/video-library/components/VideoProcessingToolbar.tsx) — 顶栏追加两个 `<ProcessingJobButton>`（icon = Languages / Grid3X3），title 标注 GPU 串行 / 礼让播放等约束；顶部注释「3 类操作」改「5 类操作」

**验证：**
- `mvn -pl tools/tool-treesize -am -DskipTests compile` BUILD SUCCESS（toolbox-common + tool-treesize 全过）
- `npm run typecheck` 通过
- 未在浏览器实测启动→进度→停止全链路（待真实视频库跑一遍）
